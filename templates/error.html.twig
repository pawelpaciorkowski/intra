<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{ defaults.title }}">
    <meta name="author" content="Alab Laboratoria">
    <title>{% block title %}{% endblock %}{{ [defaults.title]|join(' - ') }}</title>
    <link href="{{ prefetch(asset('css/alab.error.min.css')) }}" rel="stylesheet" nonce="{{ csp_nonce('script') }}">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js" nonce="{{ csp_nonce('script') }}"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js" nonce="{{ csp_nonce('script') }}"></script>
    {% block stylesheets %}{% endblock %}
    <![endif]-->
</head>
<body role="document">
<div class="container">

    {% block body %}{% endblock %}

</div>
{% block javascripts %}{% endblock %}
</body>
</html>